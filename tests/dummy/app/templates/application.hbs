<h2 id="title">Welcome to Ember</h2>

{{#if session.isAuthenticated}}
  Logged in
  {{mock-logout}}
{{else}}
  {{#mock-login as |login|}}
    {{#if login.loading}}
      loading...
    {{else}}
      {{login.errorMessage}}
      {{#login.each-account accounts=accounts as |account|}}
        {{account.user.firstName}} {{account.user.lastName}} {{account.user.group.name}}
      {{/login.each-account}}
    {{/if}}
  {{/mock-login}}
{{/if}}
{{outlet}}
